// Heimdall TypeScript SDK (T001 scaffold). Real client lands in T035.
// May import @heimdall/contracts only. Must never import server internals.
import { CONTRACTS_VERSION } from '@heimdall/contracts';

export const SDK_CONTRACTS_VERSION = CONTRACTS_VERSION;
export const SDK_VERSION = '0.0.0';
